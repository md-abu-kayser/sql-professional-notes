# Subqueries in Different Clauses

- In `SELECT`: must be scalar.
- In `FROM`: derived table (must have alias).
- In `WHERE`: with `IN`, `=`, etc.

> 📘 Next: [UNION, INTERSECT, EXCEPT](05-union-intersect-except.md)
